using UnityEngine;

public static class BallSelector
{
    // Color seleccionado por defecto (rojo oscuro, similar al material actual)
    public static Color selectedBallColor = new Color(0.23773581f, 0f, 0f, 1f);
    
    // También puedes guardar el nombre del material si prefieres usar materiales
    public static string selectedBallName = "Default";
}


